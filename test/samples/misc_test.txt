key-a = a
ref-ref-a = ${ref-a?failed}
ref-a = ${key-a}
ref-default-a = ${key-nexist?${key-a}}
ref-fallback-a = ${ .key-a ? fail }
ref-nexist = ${key-nexist? " f a i l ' }
ref-fail = ${key-nexist}
file-parse = ${file key_file.txt }
env = ${env test_env? fail}
env-nexist = ${env nexist? " f a i l " }
cmd = ${cmd echo hello}
cmd-ref = ${cmd}
wrapper.key-b = b
appender = ${save last '${rel last} eat\n${rel last}'}
appender.last = 'I'
cache = ${cache 1000 ${appender}}
cache_too_short = ${cache 0 ${appender}}
multiplier = ${save last '${rel last} ${cmd echo $((${rel source}*10))}'}
multiplier.last = 0
multiplier.source = 1
array_cache = ${array_cache 3 ${multiplier.source} ${multiplier}}
array_cache2 = ${array_cache array_cache ${multiplier.source} ${multiplier}}
clock = ${clock 1 1000 0}
